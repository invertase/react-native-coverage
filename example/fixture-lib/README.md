# CoverageFixture

Instrumented fixture native library used by the Expo `example/` harness to prove
multi-image (iOS) / Jacoco (Android) coverage via `react-native-coverage`.

Not a product API — Pattern C test-only.
